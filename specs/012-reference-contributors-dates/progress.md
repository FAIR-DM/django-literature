# Progress — 012 Manage a reference's contributors, dates and identifiers

| When | Event |
|---|---|
| 2026-08-25 | Intake on issue #48. Four questions, all answered: the contributor field completes text from stored names and always creates its own record, so accepting a completion says how a name is spelled and never who the person is; the three collections are edited on the reference form rather than as separate actions on its page; the reference's own publication date leads and the other five CSL slots stay reachable; identifier rejections diagnose as far as there is something to diagnose, which for ISBN and ISSN means telling a mistyped character from a wrong shape. Merging duplicate contributor records was split out as #112. |
| 2026-08-25 | Issue #48 accepted. |
| 2026-08-25 | `spec.md` and `decisions.md` written. Nine further ambiguities resolved without escalating, recorded in the clarification scan and in ten numbered decisions. |
| 2026-08-25 | Branch `012-reference-contributors-dates` pushed. Issue #48 promoted to the feature's parent issue; story sub-issues #113, #114, #115, #116 created; draft PR #117 opened against the v1.0.0 milestone. Issue-title lint green; stage-exit S2 green. |
| 2026-08-25 | Specification gate: **approved** by Sam, in session, with no changes requested. |
| 2026-08-25 | Design review, one round. Two blocking findings, both verified against the code and both fixed in the plan rather than deferred. The edit path would have rewritten a `Name` shared with another reference, because the import path already reuses records — the plan now creates a new record and repoints the link whenever anything else is credited on the old one (T007a). Only two of the six date slots were reachable, and a row cloned from the set's template would have saved an unvalidated empty slot — the date form now declares the slot itself (T013, T015a). Three smaller points folded in: the duplicate checks ignore rows flagged for deletion, contributor rows group by role, and the ordering mechanism gets a spike before the column is built. |
| 2026-08-25 | Foundations landed. The reference form carries the three sets of related rows, the date model enforces its own span rule, and the catalogue toolbar was repaired after the toolkit upgrade removed the hook it rendered from (T001–T005, T001a). |
| 2026-08-25 | US-1 delivered: contributors are credited by completing a name's spelling, never by linking to a stored record, and an edit never rewrites a record another reference is credited with. The set renders through the packaged component rather than a copy of it, with the two gaps that forced the copy raised as django-mvp/django-mvp#306 and #307 (T006–T012a). |
| 2026-08-25 | The session running this feature was lost to a tooling fault partway through the dates story, leaving finished, tested work uncommitted. It was reconciled rather than restarted: the tree covered T013–T019 in full and passed the suite, the lint gate and the migration check, so it was committed as it stood. |
| 2026-08-25 | US-2 delivered: every one of the six date slots is reachable, each end of a span keeps its own precision, an unreadable imported date is visible and repairable, and two rows claiming one slot are refused on the form ahead of the database constraint (T013–T019). |
| 2026-08-25 | US-3 delivered: identifiers are added and removed as rows, one per kind, with the six known kinds offered and anything else stored unchecked. A rejected ISBN now tells a mistyped character from a wrong shape, recovering a distinction the validator already computed and discarded. ISSN is untouched and its gap stays with #118 (T020–T026). |
| 2026-08-25 | US-4 delivered: the three flows were already reachable from the seeded demo with no sign-in, which is asserted rather than assumed. The walk that guards them checks each flow on its own submission and against what the reference's page then shows, and each flow was broken in turn and watched failing before being restored (T027–T030). |
| 2026-08-25 | Convergence. Every decision carries a verdict: four graduated into two new records, covering what a completion may assert about identity and how a related row survives a save, and the other fourteen are declined with a reason. The branch introduces no migration, and no pre-existing test was weakened. |
