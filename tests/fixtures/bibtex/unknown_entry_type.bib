@set{grouping_entry,
  entryset = {lecun2015deep, codd1970relational},
  title    = {A group of other entries, not a reference of its own}
}

@xdata{shared_fields_entry,
  publisher = {Fields other entries inherit, never a reference on its own},
  address   = {Amsterdam}
}
